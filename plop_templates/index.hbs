import { App } from 'vue'
import AioTheme{{properCase name}} from './src/{{name}}.vue'

AioTheme{{properCase name}}.install = (app: App): void => {
  app.component(AioTheme{{properCase name}}.name, AioTheme{{properCase name}})
}

export default AioTheme{{properCase name}}
